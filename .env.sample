# === ICP / Mainnet ===
ICP_HOST=https://ic0.app
ICP_LEDGER_CANISTER_ID=ryjl3-tyaaa-aaaaa-aaaba-cai

# === Token creation policy ===
DEPLOY_CYCLES_AMOUNT=3000000000000

# === Treasury / Controllers ===
TREASURY_ICP_WALLET_PRINCIPAL=
TREASURY_CYCLES_WALLET_CANISTER_ID=
TREASURY_DELEGATION_IDENTITY_JSON=   # (base64 or raw JSON; whichever the app expects)

# === ICRC1 Ledger WASM (PINNED) ===
ICRC_WASM_URL=https://github.com/dfinity/ic/releases/download/ledger-suite-icrc-2025-06-19/ic-icrc1-ledger.wasm.gz
ICRC_WASM_SHA256=                     # paste from the release’s SHA256SUMS

# === Optional (index canister) ===
ENABLE_INDEX_CANISTER=false
